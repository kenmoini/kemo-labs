# GitLab CE Configuration
# Copy to .env and fill in values

# Initial root password (only used on first boot)
GITLAB_ROOT_PASSWORD=changeme

# PostgreSQL password for gitlab user on shared instance
GITLAB_DB_PASSWORD=changeme

# Timezone
TZ=America/New_York
