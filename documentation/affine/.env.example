# AFFiNE Configuration
# Copy to .env and fill in values

# PostgreSQL credentials for shared instance (requires pgvector extension)
DB_USERNAME=affine
DB_PASSWORD=changeme
DB_DATABASE=affine

# Valkey/Redis password (leave empty if no auth)
REDIS_PASSWORD=

# Timezone
TZ=America/New_York
