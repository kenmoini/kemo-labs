---
services:
  pika-pki:
    image: quay.io/kenmoini/pika-pki:latest
    container_name: pika-pki
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - /opt/workdir/caas/pika-pki/data:/data:Z
    profiles:
      - interactive
    restart: unless-stopped

  nginx:
    image: docker.io/nginx:1.27-alpine
    container_name: pki-nginx
    restart: unless-stopped
    volumes:
      - /opt/workdir/caas/pika-pki/data:/data:ro,Z
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:Z
    networks:
      traefik-proxy:
      bridge42:
        ipv4_address: 192.168.42.5
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pki.rule=Host(`pki.lab.kemo.dev`)"
      - "traefik.http.routers.pki.tls=true"
      - "traefik.http.routers.pki.tls.certresolver=step-ca"
      - "traefik.http.services.pki.loadbalancer.server.port=8001"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8001/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  bridge42:
    external: true
  traefik-proxy:
    external: true
