---
services:
  pika-pki:
    image: quay.io/kenmoini/pika-pki:latest
    container_name: pika-pki
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - pki-data:/data
    profiles:
      - interactive
    restart: "no"

  nginx:
    image: docker.io/nginx:1.27-alpine
    container_name: pki-nginx
    restart: unless-stopped
    volumes:
      - pki-data:/data:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      homelab:
        ipv4_address: 192.168.62.5
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pki.rule=Host(`pki.lab.kemo.network`)"
      - "traefik.http.routers.pki.tls=true"
      - "traefik.http.routers.pki.tls.certresolver=step-ca"
      - "traefik.http.services.pki.loadbalancer.server.port=80"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  pki-data:
    name: pki-data

networks:
  homelab:
    external: true
